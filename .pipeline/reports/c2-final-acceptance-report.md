# C2 Final Acceptance Report

状态: pending_user_acceptance  
时间: 2026-05-06 13:41 Asia/Shanghai

## Cycle Summary

C2 connected VSP-Coder to real Codex sessions through the Codex app-server
surface while preserving a provider-neutral frontend/backend protocol.

Completed scope:

- Instance config, data mode, automation profiles, and no production hardcoded
  user path baseline.
- Codex app-server lifecycle, health, stdio JSON-RPC transport, and provider
  status UI.
- Codex project/session discovery from `thread/list` cwd metadata.
- Existing thread hydration through `thread/read includeTurns:true`.
- New thread creation through `thread/start`.
- Message sending through `thread/resume` + `turn/start`.
- Streaming/live patch mapping for assistant/tool deltas and turn lifecycle.
- Approval/user-input request mapping and full-auto handling.
- Command/file-change artifacts, preview endpoint, and project-root preview
  confinement.
- Interrupt, pending queue controls, and app-server recovery behavior.
- Real Codex thread rename through `thread/name/set` with refresh/restart
  persistence evidence.
- Markdown/math/safe HTML rendering and tool display polish.
- Codex model picker backed by `model/list`.
- VSP server-root self-protection for local full-auto QA.
- Disposable tmp QA fixture generator and final acceptance script.

## Automated Validation

Final validation commands are recorded in the M10 report. All required checks
passed on 2026-05-06 13:41 Asia/Shanghai:

- `npm run typecheck`
- `npm test`（50 tests）
- `npm run build`
- `git diff --check`

## Manual QA Evidence So Far

- M01-M08 accepted by user during milestone QA.
- M09 rename was verified by the user across frontend rename, browser refresh,
  and safe 4180 server restart.
- M11 frontend rendering/polish accepted by user before returning to M09/M10.

## Final Manual QA Required

Run the M10 final tmp QA script in
`.pipeline/reports/m10-tmp-qa-harness-knowledge-finalization-and-acceptance-gate.md`.

The current disposable tmp session is `019dfbcd-d545-7971-a1e2-b77667896d14`,
generated by `POST /api/qa/tmp-session` under the system temporary directory.
Do not mutate existing non-tmp Codex history during acceptance.

## Deferred Work

- Mobile soft-keyboard polish can continue as a frontend follow-up.
- Durable outbound queue across VSP server restart is out of C2 scope.
- OpenCode/Claude adapters should reuse the finalized provider protocol and add
  their own adapter mapping docs.
- Codex app-server schema changes should be handled by regenerating protocol
  knowledge and updating adapter tests.

## Acceptance Gate

Awaiting explicit user input:

- `accept` closes C2.
- Any other feedback is treated as rejection/fix request and keeps C2 open.
